<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<AnchorPane prefHeight="600.0" prefWidth="800.0"
            xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="esprit.tn.pidevrh.leave.LeaveRequestController"
            stylesheets="@leave_request.css">

    <VBox alignment="CENTER" spacing="30"
          AnchorPane.topAnchor="40" AnchorPane.bottomAnchor="40"
          AnchorPane.leftAnchor="40" AnchorPane.rightAnchor="40">

        <Label text="Demande de Congé" styleClass="title-label" />

        <GridPane hgap="20" vgap="20" styleClass="form-container">
            <columnConstraints>
                <ColumnConstraints minWidth="150" halignment="RIGHT" />
                <ColumnConstraints hgrow="ALWAYS" />
            </columnConstraints>

            <Label text="Type de Congé :" />
            <ComboBox fx:id="congeComboBox" styleClass="input-field" />

            <Label text="Autre Type :" GridPane.rowIndex="1" />
            <TextField fx:id="autreCongeField" GridPane.columnIndex="1" GridPane.rowIndex="1"
                       styleClass="input-field" disable="true" />

            <Label text="Date de Début :" GridPane.rowIndex="2" />
            <DatePicker fx:id="startDatePicker" GridPane.columnIndex="1" GridPane.rowIndex="2"
                        styleClass="date-picker" />

            <Label text="Date de Fin :" GridPane.rowIndex="3" />
            <DatePicker fx:id="endDatePicker" GridPane.columnIndex="1" GridPane.rowIndex="3"
                        styleClass="date-picker" />

            <Label text="Justification :" GridPane.rowIndex="4" />
            <TextArea fx:id="justificationField" GridPane.columnIndex="1" GridPane.rowIndex="4"
                      styleClass="text-area" />

            <HBox fx:id="buttonContainer" spacing="15" GridPane.rowIndex="5" GridPane.columnIndex="1">
                <Button fx:id="checkHolidayButton" text="Check Holiday"
                        styleClass="action-button, accent-button" />
                <Button fx:id="extractTextButton" text="Extract Text 🖹"
                        styleClass="action-button, accent-button" />
            </HBox>

            <Button fx:id="uploadButton" text="Télécharger Certificat"
                    GridPane.rowIndex="6" GridPane.columnIndex="1"
                    styleClass="action-button, upload-button" />
        </GridPane>

        <Button fx:id="submitButton" text="Soumettre la Demande"
                styleClass="action-button" />
    </VBox>
</AnchorPane>